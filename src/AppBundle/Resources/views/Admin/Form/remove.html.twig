{% extends 'AppBundle:Admin/Form:skeleton.html.twig' %}
{% block title %}{% trans %}Remove{% endtrans %} {{ form.name }} | {{ parent() }}{% endblock %}
{% block content %}
    <div class="panel-heading">
        <h3 class="panel-title">{{ icon('list-alt') }} {% trans %}Delete{% endtrans %} {{ form.name }}</h3>
    </div>
    <div class="panel-body">
        {{ form(data) }}
    </div>
{% endblock %}
